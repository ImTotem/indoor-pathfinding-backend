# composed by agent-set-composer at 2026-05-05 21:19 KST
task: "building_id, floor_id, start/end coordinates만으로 길찾기 가능한 route 계약 검토와 보강"
domain: "코딩"
roles:
  planner:
    agents:
      - name: "plan-architect"
        aspect: "route API 계약과 acceptance criteria 설계"
        rationale: "요구사항을 API spec과 구현 기준으로 정리하는 plan agent"
  builder:
    agents:
      - name: "build-developer"
        aspect: "설계 문서 기반 route API 구현"
        rationale: "설계 문서를 받아 코드 변경과 영향 범위 점검을 수행하는 build agent"
  implementation_evaluator:
    agents:
      - name: "eval-implementation"
        aspect: "구현 일치성, 테스트, 호환성 검증"
        rationale: "개발 결과를 read-only로 검증하는 eval agent"
  e2e_evaluator:
    agents:
      - name: "eval-e2e"
        aspect: "실행 서버 API 시나리오 검증"
        rationale: "실제 서비스/API를 curl로 확인하는 e2e agent"
notes:
  - "외부 앱 계약은 building_id + floor_id + start/end coordinate를 우선한다."
  - "기존 scan_id 기반 route 계약은 내부/호환 경로로 유지한다."
